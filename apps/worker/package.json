{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "dev": "dotenv -e ../../.env.development.local -- tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@abhyas/kernel": "^0.1.0",
    "@abhyas/platform": "^0.1.0",
    "@abhyas/module-scheduling": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "tsx": "^4.19.2",
    "rimraf": "^6.0.1",
    "@types/node": "^22.10.2"
  }
}
